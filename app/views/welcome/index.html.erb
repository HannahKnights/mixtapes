<div class="tape-container">
  <div class="welcome_tape">
    <h2 class="text-center tape-title">{{title}}</h2>
  </div>
</div>

<div class="create_form center-block">
  <%= form_for(@mixtape, :url => new_mixtape_path, :welcome => { :action => :post }, html: { id: 'update_name_homepage', class: 'form-horizontal', :autocomplete => "off"})  do |form| %>
  <div class='form-group'>
    <%= form.text_field :title, 'ng-model' => 'title', 'ng-init' => "title='#{escape_javascript @mixtape.title}'" %>
  </div>
  <div class='form-group'>
  <%= form.submit 'Create Your Mixtape', :class => 'btn btn-send' %>
  </div>
  <% end %>
</div>